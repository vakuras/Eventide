{
  "name": "eventide",
  "version": "0.2.3",
  "description": "Your command center for GitHub Copilot CLI — manage, search, and switch between sessions from a sleek desktop app.",
  "author": "vakuras",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build-tauri-frontend.js",
    "dev": "npm run build && npx tauri dev",
    "tauri:build": "npm run build && npx tauri build",
    "test": "vitest run"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/xterm": "^5.5.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "esbuild": "^0.27.3",
    "jsdom": "^27.0.1",
    "vitest": "^4.0.18"
  }
}
